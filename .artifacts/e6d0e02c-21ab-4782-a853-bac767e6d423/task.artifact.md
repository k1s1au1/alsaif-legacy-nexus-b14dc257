- `[/]` تطوير تحدي الخطوات (المزامنة الحقيقية والتلقائية)
    - `[ ]` تحديث `steps-challenge.tsx` ليبدأ المزامنة تلقائياً عند الدخول
    - `[ ]` إضافة مستمع (Listener) في `capacitor-client.tsx` للمزامنة عند استئناف التطبيق
    - `[ ]` تحسين منطق المزامنة ليكون أكثر واقعية (تجهيز الربط مع الحساسات)
    - `[ ]` الرفع لـ GitHub (PUSH)
- `[ ]` التحقق من عمل المزامنة التلقائية
